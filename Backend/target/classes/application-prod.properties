#application-prod.properties

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://dpg-d3kmu5vdiees739n9al0-a.singapore-postgres.render.com/budgetbee
spring.datasource.username=budgetbee_user
spring.datasource.password=IqR8bHVVVUl08a33FqUrquYZuy6ESrgP

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# Email Configuration - Using Port 465 (SSL) for Render compatibility
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=465
spring.mail.username=${BREVO_USERNAME}
spring.mail.password=${BREVO_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp-relay.brevo.com
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=${BREVO_FROM_EMAIL}

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# Frontend URL
budgetbee.frontend.url=${BUDGETBEE_FRONTEND_URL}

# Backend URL for activation links
app.activation.url=${BUDGETBEE_BACKEND_URL}